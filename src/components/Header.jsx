import React, { Fragment } from 'react'
import {render} from 'react-dom'

/**
 * Проверка того, что документация работает в header.
 */
export class Header extends React.Component{
    render(){
        return <p>Привет мир in header!</p>
    }
}